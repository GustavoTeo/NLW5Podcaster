module.exports = {
      images:{
            domains: ['storage.googleapis.com']
      }
};

//O componente de imagem do Next não entende todas as  URL's pra isso funcionar temos que colocar os domains